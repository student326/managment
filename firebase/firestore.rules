rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Firestore is NOT used for student data
    // All student records are stored in Excel files in Firebase Storage
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

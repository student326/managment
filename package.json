{
  "name": "bursar-office-system",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "deploy": "cd frontend && npm run deploy",
    "deploy:vercel": "cd frontend && vercel --prod --yes"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
